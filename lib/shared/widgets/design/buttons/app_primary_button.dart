import '../../base/buttons/base_primary_button.dart';

class AppPrimaryButton extends BasePrimaryButton {
  const AppPrimaryButton({
    super.key,
    required super.onPressed,
    required super.child,
  });
}
