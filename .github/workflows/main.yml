# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Flutter CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # 1. Configurar Flutter
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable' # Puedes cambiar a 'beta' o 'dev' si es necesario

      # 2. Obtener dependencias con flutter pub get
      - name: Install dependencies
        run: flutter pub get

      # 3. Verificar el formato de Flutter (CORREGIDO: Usando 'flutter format')
      - name: Verify formatting
        run: flutter format --output=none --set-exit-if-changed .

      # 4. Analizar el código de Flutter
      - name: Analyze project source
        run: flutter analyze

      # 5. Ejecutar las pruebas de Flutter
      - name: Run tests
        run: flutter test
